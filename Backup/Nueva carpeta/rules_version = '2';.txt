rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite la lectura y escritura en toda la base de datos.
    // ¡OJO! Esto es para desarrollo. Para producción, querrás reglas más seguras.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}